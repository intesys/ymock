/* ---------------------------------
NotFound
--------------------------------- */

import * as React from "react";

export default function NotFound({}): JSX.Element {
  return (
    <main style={{ padding: "1rem" }}>
      <p>Not found.</p>
    </main>
  );
}
